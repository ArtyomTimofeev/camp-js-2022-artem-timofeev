/** Character DTO. */
export interface CharacterDto {

  /** Character fields. */
  readonly fields: {

    /** Name. */
    readonly name: string;

  };

  /** Primary key. */
  readonly pk: number;
}
