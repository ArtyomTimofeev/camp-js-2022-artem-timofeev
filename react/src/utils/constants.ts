/** Films collection name for firebase connection. */
export const FILMS_COLLECTION_NAME = 'films';

/** Characters collection name for firebase connection. */
export const CHARACTERS_COLLECTION_NAME = 'people';

/** Planets collection name for firebase connection. */
export const PLANETS_COLLECTION_NAME = 'planets';

/** Film property - "title". */
export const TITLE_PROPERTY = 'fields.title';

/** Film property - "title". */
export const EPISODE_ID_PROPERTY = 'fields.episode_id';

/** Film property - "title". */
export const RELEASE_DATE_PROPERTY = 'fields.release_date';

/** Default sorting active field. */
export const DEFAULT_SORTING_TYPE = TITLE_PROPERTY;

/** Default debounce time. */
export const DEFAULT_DEBOUNCE_TIME = 1000;

/** Firebase symbol encoding. */
export const FIREBASE_SYMBOL_ENCODING = '\uf8ff';

/** Button text content when user is logged out. */
export const LOGIN_TEXT = 'Log in';

/** Button text content when user is logged in. */
export const LOGOUT_TEXT = 'Log out';

/** Chunk size of ids. */
export const CHUNK_SIZE_OF_IDS = 10;
